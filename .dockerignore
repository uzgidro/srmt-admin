# Ignore build artifacts
*.exe
srmt-admin
build/

# Ignore actual config files (should be mounted, contain secrets)
/config/local.yaml
/config/dev.yaml
/config/prod.yaml
/config/docker.yaml
/config/ascue.yaml
/config/reservoir.yaml

# Keep example configs (no secrets)
!/config/*.example.yaml

# Ignore documentation
docs/
*.md
LICENSE

# Ignore Git
.git/
.gitignore

# Ignore IDE
.idea/
.vscode/
*.swp
*.swo

# Ignore test files
*_test.go
coverage.out
coverage.html

# Ignore temporary files
tmp/
*.tmp

# Excel lock files (Excel creates these when a workbook is open). They live
# next to the embedded templates in internal/lib/service/excel/templates/
# and must NEVER be picked up by //go:embed *.xlsx.
~$*.xlsx
*.xlsx.tmp

# Don't ignore migrations (needed in container)
!migrations/

# Ignore Wire generated file (will be regenerated in Docker)
cmd/wire_gen.go
